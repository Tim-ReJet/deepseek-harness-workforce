/**
 * @deepseek-ai/dsh-workforce-global — the Workforce global profile bundle.
 * The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API. The patch is
 * intentionally empty today (see the patch file's own header comment):
 * this bundle exists so the `workforce-global` profile is a real, bootable
 * layer over `@deepseek-ai/dsh-base` before any Workforce-owned plugin
 * exists to populate it.
 * @module @deepseek-ai/dsh-workforce-global
 */

export {}
